/* eslint-disable no-unused-vars */
import React from 'react';
import Header from './shared/Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Home;